#include <stdio.h>
/**
 * main - prints text as it is by escaping the first quotation
 * mark. and as requirement detects,
 * Return: ends with returning 0.Hope it passes the stupid linter
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
