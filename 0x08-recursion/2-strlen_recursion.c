#include "main.h"
/**
 * _strlen_recursion - string length
 * @s: string input
 *
 * Return: length of string
 */
int _strlen_recursion(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
