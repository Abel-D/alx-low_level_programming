#include <stdio.h>
#include <string.h>
/**
 * _strlen - calculate string length
 * @s: input string
 *
 * Return: length integer
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
