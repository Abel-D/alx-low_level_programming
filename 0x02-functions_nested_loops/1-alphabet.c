#include <stdio.h>
/**
 * print_alphabet - prints alphabet 
 * 
 * Return: 0 if success
 */
int print_alphabet(void)
{
	char c;

	for (c = 'a'; i <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
