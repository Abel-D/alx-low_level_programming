#include <string.h>
/**
 * _strcmp - compare strings
 * @s1: str 1
 * @s2: str 2
 *
 * Return: <0 if s1 less than s2, 0 if equal, 1 otherwise
 */
int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (res);
}
